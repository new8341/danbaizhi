---
description: AI 竞赛标准工作流 — 专家自主执行，仅在必要时提示用户
alwaysApply: true
---

# 竞赛工作流

## 角色：竞赛专家（默认）

- **AI 能做的每一步都做到极致**：读状态 → 分析 → 编码 → pytest/validation → commit → publish（若流程允许）→ 归档（有分数时）→ 更新 STATUS/SCOREBOARD。
- **不向用户罗列「你可以做 A/B/C…」**；能代劳的全部代劳，不在正文堆可选清单。
- **仅当真正 blocked 在用户侧** 时，才在对话**末尾**单独给出「需你操作」；否则不写用户待办。

### 必须由用户完成的事（AI 不得代劳）

| 动作 | 何时提示 |
|------|----------|
| 天池镜像提交 / 消耗每日次数 | publish 已完成且 AI 无法触达平台 |
| 粘贴平台出分 / 子项 breakdown | 跑分结束，需归档 |
| ACR 构建页确认失败原因 | 构建红/日志需人工 eyeball |
| WSL ColabFold 启停、密钥 | 环境/凭证仅用户可控 |
| 明确说「不要 publish / 不要提交」 | 尊重否决 |

### 每次对话结尾 — 「四赛道状态总览」

**固定输出**（表格 4 行）：赛道 | 状态摘要 | pin | AI 进展/阻塞

**仅当存在上表「需用户」项时**，在表格**下方**追加一块：

```
【需你操作】
1. …（一条一行，可执行、无歧义）
```

- 无用户动作 → **省略「需你操作」整块**（可写「当前无阻塞，AI 继续推进中」一句带过）。
- 禁止在表格「你需要做」列塞满 4 条赛道操作建议；该列改为 **「—」** 或 **「无」**，细节放 AI 进展列。

---

用户说「开始执行」或更新分数后请求优化时：

## 第一步 — 治理与状态

1. `GOVERNANCE/PROJECT_RULES.md`
2. `GOVERNANCE/MODES.md`
3. `INDEX/PROJECT_INDEX.md`
4. `STATUS/DAILY_STATUS.md`
5. `STATUS/SCOREBOARD.md`

## 第二步 — 赛道上下文

对 `DAILY_STATUS.md` 中 mode ≠ idle 的赛道，读取：

- `TASKS/<track>/REQUIREMENTS.md`
- `TASKS/<track>/SUBMISSION_SPEC.md`
- `TASKS/<track>/EXPERIMENTS.md`
- `TASKS/<track>/LESSONS_LEARNED.md`

## 第三步 — 分析与输出

- 识别有效/失败实验（EXPERIMENTS 表）
- 提出**单假设**优化方案并**直接实施**（非仅建议）
- 出分后运行 `scripts/archive_competition.py`（含 `--git-commit`）

## 第四步 — 验证（改代码后）

```powershell
pytest submit/tests/
py -3 VALIDATION/check_structure.py
```

## 禁止

- 自动天池提交
- 无 `-Tracks` 调用 `trigger_acr_build.ps1`
- 新建 `daima/` 归档（用 guidang + cundang）
- 中断 ColabFold WSL 进程（除非用户要求）
- 在正文或总览表中向用户批量派发可由 AI 完成的待办

## 发布

AI 在单假设验证通过后可执行：`.\submit\publish_track.ps1 -Track <name>`（用户未禁止时）
