# ReDrugClip default configuration (paths only — model settings added in phase 1)

benchmark_root: document/benchmark
manifest: document/benchmark/manifest.jsonl
outputs_dir: outputs
daima_dir: daima

# Competition submission
submission:
  csv_name: result.csv
  log_name: result.log
  zip_name: result.zip
